{
  "name": "shuttle-x-svelte-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "run": "cargo shuttle run --wd shuttle",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cargo shuttle deploy --ad --wd shuttle"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.2.3",
    "vite": "^5.0.0"
  }
}
